diff --git a/lib/presentation/workout_timer_screen.dart b/lib/presentation/workout_timer_screen.dart
index 2883395..a301413 100644
--- a/lib/presentation/workout_timer_screen.dart
+++ b/lib/presentation/workout_timer_screen.dart
@@ -1,4 +1,5 @@
 import 'dart:async';
+import 'dart:io';
 import 'package:flutter/material.dart' hide Interval;
 import 'package:audioplayers/audioplayers.dart';
 import 'package:tick_coach/domain/models/interval.dart' show IntervalKind;
@@ -274,7 +275,20 @@
                   style: Theme.of(context).textTheme.bodyLarge,
                   textAlign: TextAlign.center,
                 ),
-              const Spacer(),
+              if (currentInterval.imageUri != null &&
+                  currentInterval.imageUri!.isNotEmpty)
+                Expanded(
+                  child: Padding(
+                    padding: const EdgeInsets.all(16.0),
+                    child: Image.file(
+                      File(currentInterval.imageUri!),
+                      fit: BoxFit.contain,
+                    ),
+                  ),
+                )
+              else
+                const Spacer(),
               if (currentInterval.isRepsBased)
                 Column(
                   mainAxisSize: MainAxisSize.min,