*** a/edit_workout_screen.dart
--- b/edit_workout_screen.dart
@@
   Widget build(BuildContext context) {
     final theme = Theme.of(context);
     final colorScheme = theme.colorScheme;
     final isWorkInterval = widget.interval.kind == IntervalKind.work;
     final isReps = isWorkInterval && widget.interval.isRepsBased;
-    final Color? _baseColor = isWorkInterval
-        ? colorScheme.surfaceContainerLow
-        : null;
+    // ----- ВИЗУАЛЬНЫЕ ОТЛИЧИЯ ДЛЯ ТИПОВ -----
+    Color _bgFor(IntervalKind k) {
+      switch (k) {
+        case IntervalKind.work:
+          return colorScheme.surfaceContainerHigh; // чуть насыщенней
+        case IntervalKind.rest:
+          return colorScheme.surfaceContainerLowest; // светлее
+        case IntervalKind.prepare:
+          return colorScheme.surfaceContainerLow; // между
+        default:
+          return colorScheme.surface; // дефолт
+      }
+    }
+    Color _accentFor(IntervalKind k) {
+      switch (k) {
+        case IntervalKind.work:
+          return colorScheme.primary;
+        case IntervalKind.rest:
+          return colorScheme.secondary;
+        case IntervalKind.prepare:
+          return colorScheme.tertiary;
+        default:
+          return colorScheme.outline;
+      }
+    }
+    IconData _typeIcon(IntervalKind k) {
+      switch (k) {
+        case IntervalKind.work:
+          return Icons.fitness_center;
+        case IntervalKind.rest:
+          return Icons.self_improvement;
+        case IntervalKind.prepare:
+          return Icons.timer_outlined;
+        default:
+          return Icons.hourglass_empty;
+      }
+    }
+    final Color baseColor = _bgFor(widget.interval.kind);
+    final Color accent = _accentFor(widget.interval.kind);
@@
-    return Card(
+    return Card(
       margin: const EdgeInsets.symmetric(vertical: 4),
-      // Подсветка карты во время нажатия на drag-handle
-      color: _dragPressed ? colorScheme.secondaryContainer : _baseColor,
-      child: Column(
+      // Подсветка карты во время нажатия на drag-handle
+      color: _dragPressed ? colorScheme.secondaryContainer : baseColor,
+      child: Container(
+        // Цветная полоса слева — тонкий акцент типа
+        decoration: BoxDecoration(
+          border: Border(left: BorderSide(color: accent, width: 4)),
+        ),
+        child: Column(
         crossAxisAlignment: CrossAxisAlignment.start,
         children: [
           Row(
             crossAxisAlignment: CrossAxisAlignment.start,
             children: [
@@
               Expanded(
                 child: Column(
                   crossAxisAlignment: CrossAxisAlignment.center,
                   children: [
                     Padding(
                       padding: const EdgeInsets.only(top: 12.0, bottom: 4.0),
-                      child: Text(
-                        widget.interval.title ??
-                            widget.interval.kind.name.capitalize(),
-                        style: theme.textTheme.titleMedium,
-                        textAlign: TextAlign.center,
-                      ),
+                      child: Row(
+                        mainAxisAlignment: MainAxisAlignment.center,
+                        mainAxisSize: MainAxisSize.min,
+                        children: [
+                          Icon(_typeIcon(widget.interval.kind), size: 18, color: accent),
+                          const SizedBox(width: 6),
+                          Text(
+                            widget.interval.title ??
+                                widget.interval.kind.name.capitalize(),
+                            style: theme.textTheme.titleMedium?.copyWith(
+                              color: theme.textTheme.titleMedium?.color, // читаемость
+                            ),
+                            textAlign: TextAlign.center,
+                          ),
+                        ],
+                      ),
                     ),
                     Row(
                       mainAxisAlignment: MainAxisAlignment.center,
                       children: [
                         IconButton(
